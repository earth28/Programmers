class Solution {
    public String solution(String[] arr) {
        String answer = "";
        
        for (int i = 0; i < arr.length; i++) {  // 입력받은 문자 리스트로부터 한 글자씩 가져와 answer에 누적합
            answer += arr[i];
        }
        
        return answer;
    }
}
