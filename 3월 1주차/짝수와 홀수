class Solution {
    public String solution(int num) {
        String answer = "";
        
        if (num % 2 == 0) {  // 2로 나눴을 때 나머지가 0이면 짝수
            answer = "Even";
        }
        else {
            answer = "Odd";
        }
        
        return answer;
    }
}
